name: flutter_easy_cache
description: "A don't overthink it, easy-to-use caching layer for Flutter."
version: 0.0.3
homepage:

environment:
  sdk: '>=3.2.5 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.3           # https://pub.dev/packages/shared_preferences/changelog
  flutter_secure_storage: ^9.2.2       # https://pub.dev/packages/flutter_secure_storage/changelog

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
